VIEW ConverterService_AccountHabits AS SELECT
  AccountHabits_0.ID,
  AccountHabits_0.account_ID,
  AccountHabits_0.habit_ID,
  AccountHabits_0.transaction_ID
FROM carbon_AccountHabits AS AccountHabits_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN ConverterService_Accounts AS account ON (account.ID = account_ID),
  MANY TO ONE JOIN ConverterService_Habits AS habit ON (habit.ID = habit_ID),
  MANY TO ONE JOIN ConverterService_Transactions AS transaction ON (transaction.ID = transaction_ID)
)