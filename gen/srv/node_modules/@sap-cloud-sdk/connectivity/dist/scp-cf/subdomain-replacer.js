"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getIssuerSubdomain = exports.replaceWithIssuerSubdomain = void 0;
const url_1 = require("url");
/**
 * @internal
 */
function replaceWithIssuerSubdomain(xsuaaUrl, decodedJwt) {
    const subdomain = getIssuerSubdomain(decodedJwt);
    if (!subdomain) {
        throw new Error(`Could not retrieve issuer subdomain from "iss" property: "${decodedJwt.iss}".`);
    }
    if (!isValidUrl(xsuaaUrl)) {
        throw new Error(`XSUAA URL is not a valid URL: "${xsuaaUrl}".`);
    }
    const xsuaaDomain = xsuaaUrl.slice(xsuaaUrl.indexOf('.'));
    return new url_1.URL(xsuaaUrl).protocol + '//' + subdomain + xsuaaDomain;
}
exports.replaceWithIssuerSubdomain = replaceWithIssuerSubdomain;
/**
 * @internal
 */
function getIssuerSubdomain(decodedJwt) {
    const iss = decodedJwt?.iss;
    if (iss) {
        if (!isValidUrl(iss)) {
            throw new Error(`Issuer URL in JWT is not a valid URL: "${iss}".`);
        }
        return getHost(new url_1.URL(iss)).split('.')[0];
    }
}
exports.getIssuerSubdomain = getIssuerSubdomain;
function getHost(url) {
    const { host } = url;
    if (!host || host.indexOf('.') === -1) {
        throw new Error(`Failed to determine sub-domain: invalid host in "${url}".`);
    }
    return host;
}
function isValidUrl(url) {
    try {
        new url_1.URL(url);
        return true;
    }
    catch (_) {
        return false;
    }
}
//# sourceMappingURL=subdomain-replacer.js.map