import { Cache } from './cache';
import { ClientCredentialsResponse } from './xsuaa-service-types';
/** *
 * @internal
 * @param url - URL from where the token is fetched
 * @param clientId - ClientId to fetch the token
 * @returns the token
 */
export declare function getCacheKey(url: string, clientId: string): string | undefined;
/**
 * @internal
 */
export declare const clientCredentialsTokenCache: {
    getToken: (url: any, clientId: string) => ClientCredentialsResponse | undefined;
    cacheToken: (url: any, clientId: string, token: ClientCredentialsResponse) => void;
    clear: () => void;
    getCacheInstance: () => Cache<ClientCredentialsResponse>;
};
